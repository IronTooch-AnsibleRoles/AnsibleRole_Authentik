galaxy_info:
  role_name: authentik
  namespace: irontooch
  author: IronTooch
  description: A role to install the Authentik LDAP Service for Authentication and Authorization
  license: GPL-3.0-only
  min_ansible_version: 2.1

  platforms:
    - name: Ubuntu
      versions:
        - focal

  galaxy_tags: []

dependencies:
  - 'irontooch.docker'
